# FastAPI backend dependencies
python-dotenv>=1.0
psycopg[binary]>=3.1
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0
pydantic>=2.5
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
PyJWT>=2.8
slowapi>=0.1.9
pandas>=2.0

# Shared toolkit — api/database uses astk.db.make_engine and /health uses
# astk.db.healthcheck. astk is a PRIVATE repo; the image installs it via a
# BuildKit secret named ASTK_PAT (a GitHub token) — see api/Dockerfile and
# docker-compose.yml. Non-Docker installs need git credentials for the private
# repo on PATH, or astk already importable (e.g. on PYTHONPATH).
analytics-service-toolkit @ git+https://github.com/nassim0014/analytics-service-toolkit@main
