# Airflow-specific dependencies
# NOTE: apache-airflow is NOT listed here — it's pre-installed in the
# base image (apache/airflow:2.9.3-python3.11). Listing it here would
# cause pip to try to reinstall/upgrade it, breaking the Airflow installation.

# These are the ONLY extra packages the Airflow containers need beyond
# what the base image already provides.
python-dotenv>=1.0
psycopg[binary]>=3.1
SQLAlchemy>=2.0
pandas>=2.0
numpy>=1.24
requests>=2.31
