[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "kinz-margin-guardian"
version = "1.0.0"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["src"]
